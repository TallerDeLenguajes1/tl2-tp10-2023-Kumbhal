@{ 
    ViewData["Title"] = "Crear Usuario";
}
@using tl2_tp10_2023_Kumbhal.ViewModels
@model CrearUsuarioViewModel

<h1>@ViewData["Title"]</h1>
<form asp-controller="Usuario" asp-action="Create">
    <div class="form-group">
        <label for="NombreDeUsuario">Nombre de usuario</label>
        <input asp-for = "@Model.CUVMUsuario" class="form-control" id="NombreDeUsuario">
    </div>
    <div class="form-group">
        <label for="contrasenia">Contraseña</label>
        <input asp-for = "@Model.CUVMContrasenia" class="form-control" id="contrasenia">
    </div>
    <button type="submit" class="btn btn-primary">Crear</button>
</form>
@section Scripts{<partial name="_ValidationScriptsPartial"/>}